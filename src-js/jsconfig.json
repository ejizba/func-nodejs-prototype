{
    // NOTE: This file is necessary to get intellisense working in js files during the prototype phase
    // but it should *just work* without the file once it's the "real deal"
    "compilerOptions": {
        "baseUrl": "../",
        "paths": {
            "@azure/functions-new": [
                "types"
            ]
        }
    }
}